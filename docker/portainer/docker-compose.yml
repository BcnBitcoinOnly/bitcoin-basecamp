version: '3'
services:
  app:
    image: portainer/portainer-ce:latest
    container_name: portainer
#    command: -H unix:///var/run/docker.sock
    restart: unless-stopped
#    security_opt:
#      - no-new-privileges:true
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATADIR}/data:/data
#    ports:
#      - 8000:8000
#      - 9000:9000
#If no proxy used, host is the easiest way to deploy the container
#    network_mode: host
    networks: 
      - npm-nw

volumes:
  data:

networks:
  npm-nw:
    external: true
